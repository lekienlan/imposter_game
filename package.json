{
  "name": "imposter-game",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,server \"npm run dev --workspace @imposter/web\" \"npm run dev --workspace @imposter/server\"",
    "dev:host": "concurrently -n web,server \"npm run dev:host --workspace @imposter/web\" \"npm run dev --workspace @imposter/server\"",
    "build": "npm run build --workspaces",
    "build:web": "npm run build --workspace @imposter/web",
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm run test --workspace @imposter/server",
    "deploy:web": "npm run build:web && firebase deploy --only hosting"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
