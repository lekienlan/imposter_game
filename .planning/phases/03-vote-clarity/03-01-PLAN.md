---
phase: 03-vote-clarity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/presentation/locales/en.json
  - apps/web/src/presentation/locales/vi.json
  - apps/web/src/presentation/locales/ko.json
  - apps/web/src/presentation/locales/zh.json
  - apps/web/src/presentation/styles/uxEnhancements.css
autonomous: true
requirements:
  - VOTE-01
  - VOTE-02

must_haves:
  truths:
    - "All UI text for Phase 3 is translatable via t() keys — no hardcoded strings in JSX"
    - "CSS classes for vote card selection and non-host badge exist and are named correctly"
    - "Vote card visual states (default, hover, selected, eliminated, disabled) are defined via CSS"
  artifacts:
    - path: "apps/web/src/presentation/locales/en.json"
      provides: "English i18n keys for hostVotingForGroup and voteFor"
      contains: "hostVotingForGroup"
    - path: "apps/web/src/presentation/locales/vi.json"
      provides: "Vietnamese i18n keys"
      contains: "hostVotingForGroup"
    - path: "apps/web/src/presentation/locales/ko.json"
      provides: "Korean i18n keys"
      contains: "hostVotingForGroup"
    - path: "apps/web/src/presentation/locales/zh.json"
      provides: "Chinese i18n keys"
      contains: "hostVotingForGroup"
    - path: "apps/web/src/presentation/styles/uxEnhancements.css"
      provides: "CSS classes for vote cards and non-host badge"
      contains: "arcade-vote-card"
  key_links:
    - from: "apps/web/src/presentation/voting/VotingPanel.tsx"
      to: "apps/web/src/presentation/locales/en.json"
      via: "t('game.hostVotingForGroup') and t('game.voteFor')"
      pattern: "hostVotingForGroup|voteFor"
    - from: "apps/web/src/presentation/voting/VotingPanel.tsx"
      to: "apps/web/src/presentation/styles/uxEnhancements.css"
      via: "className='arcade-vote-card'"
      pattern: "arcade-vote-card|arcade-non-host-badge"
---

<objective>
Add the i18n string keys and CSS class definitions that Plan 02's VotingPanel rewrite will consume.

Purpose: Establish string and style contracts before the component is rewritten. This prevents hardcoded strings in JSX and ensures CSS class names are defined before they are referenced.
Output: Two new i18n keys added to all 4 locale files; six new CSS classes added to uxEnhancements.css.
</objective>

<execution_context>
@/Users/lanle98/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lanle98/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-vote-clarity/03-CONTEXT.md
@.planning/phases/03-vote-clarity/03-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From apps/web/src/presentation/locales/en.json (game section, relevant excerpt):
```json
"game": {
  "waitingForHostVote": "Waiting for host to vote...",
  "castVote": "Cast Vote",
  "submitVote": "Submit Vote",
  "skip": "Skip"
}
```
New keys to add into the "game" object (all 4 locale files):
- "hostVotingForGroup" — explains WHY the UI is locked for non-host
- "voteFor" — prefix for the dynamic Submit button label ("Vote out [Name]")

From apps/web/src/presentation/styles/uxEnhancements.css (existing relevant classes):
```css
/* Already exists — do NOT redefine */
.arcade-vote-grid { display: flex; flex-wrap: wrap; gap: 6px; }
.arcade-player-eliminated { opacity: 0.4; }
.arcade-btn:disabled { opacity: 0.5; cursor: not-allowed; }
```
New classes to add (do NOT reuse .arcade-vote-grid — name conflict risk):
- .arcade-vote-card-grid (container)
- .arcade-vote-card (default state)
- .arcade-vote-card:hover:not(:disabled)
- .arcade-vote-card--selected
- .arcade-vote-card--eliminated
- .arcade-non-host-badge
- .arcade-non-host-badge__icon
- .arcade-non-host-badge__text
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n keys to all 4 locale files</name>
  <files>
    apps/web/src/presentation/locales/en.json
    apps/web/src/presentation/locales/vi.json
    apps/web/src/presentation/locales/ko.json
    apps/web/src/presentation/locales/zh.json
  </files>
  <action>
Add two keys into the "game" object of each locale file. Insert them after the existing "waitingForHostVote" key for logical grouping.

en.json — add after "waitingForHostVote":
```json
"hostVotingForGroup": "Host is voting on behalf of the group",
"voteFor": "Vote out"
```

vi.json — add after "waitingForHostVote":
```json
"hostVotingForGroup": "Host đang vote cho cả nhóm",
"voteFor": "Vote loại"
```

ko.json — add after "waitingForHostVote" (note: ko.json does not have "noStatement" key — insert after "waitingForHostVote"):
```json
"hostVotingForGroup": "호스트가 그룹을 대신해 투표 중입니다",
"voteFor": "투표"
```

zh.json — add after "waitingForHostVote":
```json
"hostVotingForGroup": "主持人正在代表小组投票",
"voteFor": "投票淘汰"
```

IMPORTANT: Maintain valid JSON. Each new key must end with a comma if a key follows it. Do not add a trailing comma after the last key in the object.
  </action>
  <verify>
    <automated>cd /Users/lanle98/Desktop/Coding/imposter_game && node -e "['en','vi','ko','zh'].forEach(l => { const f = require('./apps/web/src/presentation/locales/' + l + '.json'); if (!f.game.hostVotingForGroup || !f.game.voteFor) throw new Error('Missing key in ' + l); console.log(l + ': OK'); })"</automated>
  </verify>
  <done>All 4 locale files contain valid JSON with "game.hostVotingForGroup" and "game.voteFor" keys populated in their respective languages.</done>
</task>

<task type="auto">
  <name>Task 2: Add vote card and non-host badge CSS classes to uxEnhancements.css</name>
  <files>apps/web/src/presentation/styles/uxEnhancements.css</files>
  <action>
Append the following CSS block to the END of uxEnhancements.css. Do NOT modify existing classes.

Use the existing design token variable names from AppColor.ts. The file already uses:
- var(--blue-500), var(--blue-600), var(--blue-900)
- var(--yellow-300), var(--yellow-400), var(--yellow-700), var(--yellow-900)
- var(--neutral-white), var(--neutral-black), var(--neutral-400), var(--neutral-700)
- var(--surface-primary)

Append this block:

```css
/* ── Phase 3: Vote Clarity ───────────────────────────── */

/* Non-host voting badge — static state indicator, no spinner, no animation */
.arcade-non-host-badge {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  padding: 16px;
  border: 3px solid var(--blue-500);
  background: color-mix(in srgb, var(--surface-primary) 88%, var(--blue-900));
  text-align: center;
}

.arcade-non-host-badge__icon {
  font-size: 0.75rem;
  letter-spacing: 0.1em;
  color: var(--yellow-300);
  border: 2px solid var(--yellow-400);
  padding: 2px 8px;
}

.arcade-non-host-badge__text {
  margin: 0;
  color: var(--blue-100);
  letter-spacing: 0.06em;
  font-size: 0.9rem;
}

/* Vote target card grid — host only */
.arcade-vote-card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
  gap: 8px;
}

/* Vote target card — base state */
.arcade-vote-card {
  display: block;
  width: 100%;
  padding: 12px 8px;
  border: 3px solid var(--blue-600);
  background: color-mix(in srgb, var(--surface-primary) 86%, var(--blue-900));
  color: var(--neutral-white);
  font: inherit;
  font-weight: 700;
  letter-spacing: 0.06em;
  text-align: center;
  cursor: pointer;
  transition: none;
}

.arcade-vote-card:hover:not(:disabled) {
  border-color: var(--yellow-400);
  background: color-mix(in srgb, var(--surface-primary) 80%, var(--yellow-900));
}

/* Selected state — prominent, cannot be missed */
.arcade-vote-card--selected {
  border-color: var(--yellow-400);
  background: color-mix(in srgb, var(--surface-primary) 72%, var(--yellow-900));
  color: var(--yellow-300);
  box-shadow: 0 0 0 2px var(--yellow-400);
}

/* Eliminated player — dimmed, unclickable */
.arcade-vote-card--eliminated {
  opacity: 0.35;
  cursor: not-allowed;
}

.arcade-vote-card:disabled {
  cursor: not-allowed;
}
```

IMPORTANT: Do not add a CSS comment character in the middle of a property value. The `color-mix()` calls are valid CSS — do not modify them.
  </action>
  <verify>
    <automated>cd /Users/lanle98/Desktop/Coding/imposter_game && grep -c "arcade-vote-card" apps/web/src/presentation/styles/uxEnhancements.css | xargs -I{} test {} -ge 5 && echo "CSS classes OK" && grep "arcade-non-host-badge" apps/web/src/presentation/styles/uxEnhancements.css | head -1</automated>
  </verify>
  <done>uxEnhancements.css ends with the new block; grep finds at least 5 occurrences of "arcade-vote-card"; grep finds "arcade-non-host-badge". File does not exceed 300 lines. Existing classes are unchanged.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Node JSON parse check passes for all 4 locale files (no syntax errors)
2. All 4 locale files have game.hostVotingForGroup and game.voteFor
3. uxEnhancements.css has all new classes appended without modifying existing ones
4. uxEnhancements.css is under 300 lines
</verification>

<success_criteria>
- t('game.hostVotingForGroup') resolves in all 4 locales
- t('game.voteFor') resolves in all 4 locales
- .arcade-vote-card, .arcade-vote-card--selected, .arcade-vote-card--eliminated, .arcade-non-host-badge CSS classes exist in uxEnhancements.css
- No existing CSS classes were modified or removed
- All JSON files remain valid (parseable by Node)
</success_criteria>

<output>
After completion, create `.planning/phases/03-vote-clarity/03-01-SUMMARY.md`
</output>
