---
phase: 02-phase-clarity
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/web/src/presentation/shared/phasePresentation.ts
  - apps/web/src/domain/__tests__/phasePresentation.test.ts
  - apps/web/src/presentation/locales/vi.json
  - apps/web/src/presentation/locales/en.json
  - apps/web/src/presentation/locales/ko.json
  - apps/web/src/presentation/locales/zh.json
autonomous: true
requirements:
  - PHASE-01
  - PHASE-02

must_haves:
  truths:
    - "phaseLabel maps every Phase enum value to a non-empty i18n key string"
    - "phaseGuidanceKey returns a role-differentiated i18n key string for every Phase and both host/non-host roles"
    - "All 4 locale files (vi, en, ko, zh) contain the same key structure under phase.label.* and phase.guidance.*"
    - "Old phaseGuide object with hardcoded English strings no longer exported from phasePresentation.ts"
  artifacts:
    - path: "apps/web/src/domain/__tests__/phasePresentation.test.ts"
      provides: "Unit tests for phaseLabel and phaseGuidanceKey"
      exports: ["describe('phaseLabel')", "describe('phaseGuidanceKey')"]
    - path: "apps/web/src/presentation/shared/phasePresentation.ts"
      provides: "phaseLabel map, phaseGuidanceKey function, phaseTone map"
      exports: ["phaseLabel", "phaseGuidanceKey", "phaseTone"]
    - path: "apps/web/src/presentation/locales/vi.json"
      provides: "Vietnamese translations for phase.label.* and phase.guidance.host.* / phase.guidance.player.*"
      contains: "phase.label.waiting"
    - path: "apps/web/src/presentation/locales/en.json"
      provides: "English translations (same key structure)"
      contains: "phase.label.waiting"
    - path: "apps/web/src/presentation/locales/ko.json"
      provides: "Korean translations (same key structure)"
      contains: "phase.label.waiting"
    - path: "apps/web/src/presentation/locales/zh.json"
      provides: "Chinese translations (same key structure)"
      contains: "phase.label.waiting"
  key_links:
    - from: "phasePresentation.ts phaseLabel"
      to: "vi/en/ko/zh.json phase.label.*"
      via: "i18n key string returned matches key path in JSON"
      pattern: "phase\\.label\\."
    - from: "phasePresentation.ts phaseGuidanceKey"
      to: "vi/en/ko/zh.json phase.guidance.host.* / phase.guidance.player.*"
      via: "i18n key string returned matches key path in JSON"
      pattern: "phase\\.guidance\\.(host|player)\\."
---

<objective>
Build and test the logic layer for Phase Clarity: replace the hardcoded English `phaseGuide` object in `phasePresentation.ts` with two pure functions (`phaseLabel`, `phaseGuidanceKey`) that return i18n key strings, then populate all four locale JSON files with the matching key structure.

Purpose: Establish the single source of truth for phase-to-i18n-key mapping. ActionBoard (Plan 02) will consume these exports without any inline logic.
Output: Updated `phasePresentation.ts`, passing unit tests, and all four locale JSON files with new keys.
</objective>

<execution_context>
@/Users/lanle98/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lanle98/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-phase-clarity/02-CONTEXT.md
@.planning/phases/02-phase-clarity/02-RESEARCH.md

<interfaces>
<!-- Current state of phasePresentation.ts (73 lines — will be modified) -->
```typescript
// apps/web/src/presentation/shared/phasePresentation.ts (current, pre-modification)
import { Phase } from '@imposter/shared';

export const phaseTone: Record<Phase, string> = {
  [Phase.GAME_CREATION]: 'text-phase-lobby',
  [Phase.WAITING_FOR_PLAYERS]: 'text-phase-lobby',
  [Phase.LOBBY_READY]: 'text-phase-reveal',
  [Phase.ROLE_DISTRIBUTION]: 'text-phase-reveal',
  [Phase.ROUND_DESCRIPTION]: 'text-phase-description',
  [Phase.ROUND_DISCUSSION]: 'text-phase-discussion',
  [Phase.ROUND_VOTING]: 'text-phase-voting',
  [Phase.ROUND_RESULT]: 'text-phase-elimination',
  [Phase.WHITE_TRANSITION]: 'text-phase-elimination',
  [Phase.WIN_LOSE_CHECK]: 'text-phase-voting',
  [Phase.GAME_ENDED]: 'text-phase-over',
};

export const phaseGuide: Record<Phase, { title: string; description: string; tip: string }> = {
  // ... hardcoded English strings (56 lines) — DELETE ENTIRELY
};
```

<!-- Phase enum — all 11 values -->
```typescript
// packages/shared/src/enums/Phase.ts
export enum Phase {
  GAME_CREATION = 'GAME_CREATION',
  WAITING_FOR_PLAYERS = 'WAITING_FOR_PLAYERS',
  LOBBY_READY = 'LOBBY_READY',
  ROLE_DISTRIBUTION = 'ROLE_DISTRIBUTION',
  ROUND_DESCRIPTION = 'ROUND_DESCRIPTION',
  ROUND_DISCUSSION = 'ROUND_DISCUSSION',
  ROUND_VOTING = 'ROUND_VOTING',
  ROUND_RESULT = 'ROUND_RESULT',
  WHITE_TRANSITION = 'WHITE_TRANSITION',
  WIN_LOSE_CHECK = 'WIN_LOSE_CHECK',
  GAME_ENDED = 'GAME_ENDED',
}
```

<!-- Existing test pattern (roleRevealTrigger.test.ts) — follow this style -->
```typescript
import { describe, test, expect } from 'vitest';
import { Phase } from '@imposter/shared';

describe('feature name', () => {
  test('describes expected behavior', () => {
    expect(myFunction(Phase.ROUND_VOTING, true)).toBe('expected.key');
  });
});
```

<!-- Locale JSON structure (vi.json, en.json, ko.json, zh.json) — add "phase" sibling section -->
// Current top-level keys: "lobby", "game", "app", "room", "wordPopup", "roomPreview", "role"
// Add new "phase" key with "label" and "guidance" sub-objects
</interfaces>
</context>

<feature>
  <name>Phase label and guidance key mapping</name>
  <files>
    apps/web/src/presentation/shared/phasePresentation.ts
    apps/web/src/domain/__tests__/phasePresentation.test.ts
    apps/web/src/presentation/locales/vi.json
    apps/web/src/presentation/locales/en.json
    apps/web/src/presentation/locales/ko.json
    apps/web/src/presentation/locales/zh.json
  </files>
  <behavior>
    phaseLabel: maps every Phase enum value to an i18n key string.
    - GAME_CREATION, WAITING_FOR_PLAYERS, LOBBY_READY → 'phase.label.waiting'
    - ROLE_DISTRIBUTION → 'phase.label.wordReveal'
    - ROUND_DESCRIPTION, ROUND_DISCUSSION, WHITE_TRANSITION → 'phase.label.discussion'
    - ROUND_VOTING, WIN_LOSE_CHECK → 'phase.label.voting'
    - ROUND_RESULT → 'phase.label.voting'
    - GAME_ENDED → 'phase.label.ended'

    phaseGuidanceKey(phase, isHost): returns role-differentiated i18n key.
    - Format: 'phase.guidance.{host|player}.{slot}'
    - Slot mapping:
      GAME_CREATION / WAITING_FOR_PLAYERS / LOBBY_READY → 'waiting'
      ROLE_DISTRIBUTION → 'wordReveal'
      ROUND_DESCRIPTION → 'description'
      ROUND_DISCUSSION / WHITE_TRANSITION → 'discussion'
      ROUND_VOTING / WIN_LOSE_CHECK → 'voting'
      ROUND_RESULT → 'result'
      GAME_ENDED → 'ended'

    Test cases (input → expected output):
    - phaseLabel[Phase.GAME_ENDED] → 'phase.label.ended'
    - phaseLabel[Phase.ROUND_VOTING] → 'phase.label.voting'
    - phaseLabel[every Phase value] → truthy string
    - phaseGuidanceKey(Phase.ROUND_VOTING, true) → 'phase.guidance.host.voting'
    - phaseGuidanceKey(Phase.ROUND_VOTING, false) → 'phase.guidance.player.voting'
    - phaseGuidanceKey(Phase.WAITING_FOR_PLAYERS, true) → 'phase.guidance.host.waiting'
    - phaseGuidanceKey(every Phase, true) → truthy string
    - phaseGuidanceKey(every Phase, false) → truthy string
  </behavior>
  <implementation>
    RED phase:
    1. Create test file at apps/web/src/domain/__tests__/phasePresentation.test.ts
    2. Import phaseLabel and phaseGuidanceKey from '../../presentation/shared/phasePresentation'
    3. Write describe blocks as specified in behavior above
    4. Run: cd /Users/lanle98/Desktop/Coding/imposter_game/apps/web && yarn test --run 2>&1 | grep -A5 "phasePresentation"
    5. Confirm tests FAIL (functions do not exist yet)

    GREEN phase:
    1. Open apps/web/src/presentation/shared/phasePresentation.ts
    2. DELETE the entire phaseGuide export (lines 17-73 in current file)
    3. ADD phaseLabel: Record&lt;Phase, string&gt; — map all 11 Phase values to label keys per behavior above
    4. ADD phaseGuidanceKey(phase: Phase, isHost: boolean): string — use role const + slot map per behavior above
    5. Keep phaseTone unchanged
    6. Run: cd /Users/lanle98/Desktop/Coding/imposter_game/apps/web && yarn test --run 2>&1 | grep -A5 "phasePresentation"
    7. Confirm all tests PASS

    Locale JSON phase (run after GREEN):
    Add a "phase" key to all four locale files. Structure:
    {
      "phase": {
        "label": {
          "waiting": "[locale translation]",
          "wordReveal": "[locale translation]",
          "discussion": "[locale translation]",
          "voting": "[locale translation]",
          "ended": "[locale translation]"
        },
        "guidance": {
          "host": {
            "waiting": "[locale translation]",
            "wordReveal": "[locale translation]",
            "description": "[locale translation]",
            "discussion": "[locale translation]",
            "voting": "[locale translation]",
            "result": "[locale translation]",
            "ended": "[locale translation]"
          },
          "player": {
            "waiting": "[locale translation]",
            "wordReveal": "[locale translation]",
            "description": "[locale translation]",
            "discussion": "[locale translation]",
            "voting": "[locale translation]",
            "result": "[locale translation]",
            "ended": "[locale translation]"
          }
        }
      }
    }

    Exact strings by locale:
    vi.json — locked decisions specify wording for waiting and voting; others use Claude's discretion:
      label.waiting: "Chờ"
      label.wordReveal: "Tiết lộ từ"
      label.discussion: "Thảo luận"
      label.voting: "Bầu chọn"
      label.ended: "Kết thúc"
      guidance.host.waiting: "Nhấn Start khi đủ người chơi"
      guidance.host.wordReveal: "Đợi người chơi xem từ bí mật"
      guidance.host.description: "Theo dõi lượt phát biểu"
      guidance.host.discussion: "Thảo luận cùng người chơi"
      guidance.host.voting: "Chọn người bị nghi ngờ"
      guidance.host.result: "Xem kết quả vòng này"
      guidance.host.ended: "Trò chơi đã kết thúc"
      guidance.player.waiting: "Chờ host bắt đầu"
      guidance.player.wordReveal: "Xem từ bí mật của bạn"
      guidance.player.description: "Chuẩn bị phát biểu của bạn"
      guidance.player.discussion: "Thảo luận cùng mọi người"
      guidance.player.voting: "Đang chờ host bầu chọn"
      guidance.player.result: "Xem kết quả vòng này"
      guidance.player.ended: "Trò chơi đã kết thúc"

    en.json:
      label.waiting: "Waiting"
      label.wordReveal: "Word Reveal"
      label.discussion: "Discussion"
      label.voting: "Voting"
      label.ended: "Game Over"
      guidance.host.waiting: "Start when all players have joined"
      guidance.host.wordReveal: "Wait for players to see their word"
      guidance.host.description: "Listen to each player's statement"
      guidance.host.discussion: "Discuss with your players"
      guidance.host.voting: "Choose who you suspect"
      guidance.host.result: "Review this round's result"
      guidance.host.ended: "The game has ended"
      guidance.player.waiting: "Wait for the host to start"
      guidance.player.wordReveal: "Check your secret word"
      guidance.player.description: "Prepare your statement"
      guidance.player.discussion: "Discuss with everyone"
      guidance.player.voting: "Waiting for the host to vote"
      guidance.player.result: "Review this round's result"
      guidance.player.ended: "The game has ended"

    ko.json:
      label.waiting: "대기 중"
      label.wordReveal: "단어 공개"
      label.discussion: "토론"
      label.voting: "투표"
      label.ended: "게임 종료"
      guidance.host.waiting: "모든 플레이어가 참가하면 시작하세요"
      guidance.host.wordReveal: "플레이어가 단어를 확인할 때까지 기다리세요"
      guidance.host.description: "각 플레이어의 발언을 들으세요"
      guidance.host.discussion: "플레이어들과 토론하세요"
      guidance.host.voting: "의심스러운 사람을 선택하세요"
      guidance.host.result: "이번 라운드 결과를 확인하세요"
      guidance.host.ended: "게임이 종료되었습니다"
      guidance.player.waiting: "호스트가 시작할 때까지 기다리세요"
      guidance.player.wordReveal: "비밀 단어를 확인하세요"
      guidance.player.description: "발언을 준비하세요"
      guidance.player.discussion: "모두와 토론하세요"
      guidance.player.voting: "호스트가 투표 중입니다"
      guidance.player.result: "이번 라운드 결과를 확인하세요"
      guidance.player.ended: "게임이 종료되었습니다"

    zh.json:
      label.waiting: "等待中"
      label.wordReveal: "揭示词语"
      label.discussion: "讨论"
      label.voting: "投票"
      label.ended: "游戏结束"
      guidance.host.waiting: "所有玩家加入后开始游戏"
      guidance.host.wordReveal: "等待玩家查看他们的词语"
      guidance.host.description: "聆听每位玩家的发言"
      guidance.host.discussion: "与玩家讨论"
      guidance.host.voting: "选择你怀疑的人"
      guidance.host.result: "查看本轮结果"
      guidance.host.ended: "游戏已结束"
      guidance.player.waiting: "等待主机开始"
      guidance.player.wordReveal: "查看你的秘密词语"
      guidance.player.description: "准备你的发言"
      guidance.player.discussion: "与大家讨论"
      guidance.player.voting: "等待主机投票"
      guidance.player.result: "查看本轮结果"
      guidance.player.ended: "游戏已结束"

    IMPORTANT — do NOT use the old phaseGuide pattern:
    - phaseGuidanceKey MUST return i18n key strings like "phase.guidance.host.waiting" — NOT translated text
    - phaseLabel MUST return key strings like "phase.label.waiting" — NOT display text
    - Wrapping existing English strings as t() keys is WRONG (see Research pitfall 1)
  </implementation>
</feature>

<verification>
cd /Users/lanle98/Desktop/Coding/imposter_game/apps/web && yarn test --run 2>&1 | tail -20
</verification>

<success_criteria>
- All tests in phasePresentation.test.ts pass (GREEN)
- phaseLabel and phaseGuidanceKey are exported from phasePresentation.ts; phaseGuide is not
- phasePresentation.ts is under 300 lines
- All 4 locale JSON files contain "phase.label.*" and "phase.guidance.host.*" and "phase.guidance.player.*" keys
- yarn test --run exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-phase-clarity/02-01-SUMMARY.md`
</output>
