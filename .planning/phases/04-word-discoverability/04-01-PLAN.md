---
phase: 04-word-discoverability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/presentation/word-reveal/WordRevealPopup.tsx
  - apps/web/src/presentation/styles/components.css
  - apps/web/src/presentation/locales/en.json
  - apps/web/src/presentation/locales/vi.json
  - apps/web/src/presentation/locales/ko.json
  - apps/web/src/presentation/locales/zh.json
autonomous: true
requirements: [WORD-01]

must_haves:
  truths:
    - "When player taps Ready on the Word Reveal popup, a closing cue message appears before the popup dismisses"
    - "i18n keys exist for closing cue text, eye icon label, and word masked placeholder in all 4 locales"
    - "CSS classes exist for word-block pulse animation, eye icon button, and mini word popup"
  artifacts:
    - path: "apps/web/src/presentation/word-reveal/WordRevealPopup.tsx"
      provides: "Closing cue state and delayed onClose with cleanup"
      contains: "isClosing"
    - path: "apps/web/src/presentation/styles/components.css"
      provides: "Pulse keyframes, eye icon button, mini popup styles"
      contains: "word-pulse"
    - path: "apps/web/src/presentation/locales/en.json"
      provides: "closingCue, peekWord, wordMasked i18n keys"
      contains: "closingCue"
  key_links:
    - from: "WordRevealPopup.tsx"
      to: "locales/*.json"
      via: "t('wordPopup.closingCue')"
      pattern: "wordPopup\\.closingCue"
---

<objective>
Add the foundational assets for word discoverability: closing cue on WordRevealPopup, i18n keys for all new strings, and CSS classes for the pulse animation, eye icon button, and mini word popup.

Purpose: Lay the groundwork (i18n + CSS + closing cue behavior) so Plan 02 can wire the eye icon and mini popup into ViewerCard without also juggling styles and translations.
Output: Updated WordRevealPopup with closing cue, 4 updated locale files, extended components.css.
</objective>

<execution_context>
@/Users/lanle98/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lanle98/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-word-discoverability/04-CONTEXT.md
@.planning/phases/04-word-discoverability/04-RESEARCH.md

@apps/web/src/presentation/word-reveal/WordRevealPopup.tsx
@apps/web/src/presentation/styles/components.css
@apps/web/src/presentation/locales/en.json

<interfaces>
From apps/web/src/presentation/word-reveal/WordRevealPopup.tsx:
```typescript
interface Props {
  word: string;
  onClose: () => void;
}
export const WordRevealPopup = ({ word, onClose }: Props) => { ... }
```

From apps/web/src/presentation/locales/en.json (relevant section):
```json
{
  "wordPopup": {
    "ready": "Ready!"
  },
  "game": {
    "word": "Word",
    "wordLocked": "Word will be revealed when the game starts"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n keys and CSS classes for word discoverability</name>
  <files>
    apps/web/src/presentation/locales/en.json
    apps/web/src/presentation/locales/vi.json
    apps/web/src/presentation/locales/ko.json
    apps/web/src/presentation/locales/zh.json
    apps/web/src/presentation/styles/components.css
  </files>
  <action>
    **i18n keys to add in all 4 locale files:**

    In the `wordPopup` section (after `ready`):
    - `wordPopup.closingCue` — en: "Word saved to your card", vi: "Tu da duoc luu vao the cua ban", ko: "단어가 카드에 저장되었습니다", zh: "单词已保存到你的卡片"

    In the `game` section:
    - `game.peekWord` — en: "Peek at your word", vi: "Xem lai tu bi mat", ko: "단어 엿보기", zh: "查看你的单词"
    - `game.wordMasked` — en: "***", vi: "***", ko: "***", zh: "***"

    Use proper Vietnamese diacritics for vi.json (check existing vi.json for accent style).

    **CSS to append at end of components.css:**

    1. Pulse keyframe animation for word block after popup closes:
    ```css
    @keyframes word-pulse {
      0% { box-shadow: 0 0 0 0 color-mix(in srgb, var(--yellow-400) 70%, transparent); }
      50% { box-shadow: 0 0 12px 4px color-mix(in srgb, var(--yellow-400) 70%, transparent); }
      100% { box-shadow: 0 0 0 0 color-mix(in srgb, var(--yellow-400) 70%, transparent); }
    }
    .arcade-viewer-word-block--pulse {
      animation: word-pulse 1.5s ease-out 1;
    }
    ```

    2. Eye icon button (positioned top-right of word block):
    ```css
    .arcade-eye-btn {
      position: absolute;
      top: 8px;
      right: 8px;
      background: none;
      border: none;
      cursor: pointer;
      font-size: 1.2rem;
      opacity: 0.7;
      transition: opacity 0.2s;
      color: var(--yellow-300);
      padding: 2px 4px;
      line-height: 1;
    }
    .arcade-eye-btn:hover {
      opacity: 1;
    }
    ```

    3. Mini word popup (positioned below word block, centered):
    ```css
    .arcade-mini-word-popup {
      position: absolute;
      top: 100%;
      left: 50%;
      transform: translateX(-50%);
      margin-top: 6px;
      padding: 8px 14px;
      background: color-mix(in srgb, var(--surface-primary) 95%, var(--yellow-900));
      border: 2px solid var(--yellow-400);
      color: var(--yellow-300);
      font-weight: 700;
      font-size: 1rem;
      letter-spacing: 0.1em;
      white-space: nowrap;
      z-index: 10;
      animation: mini-popup-in 0.2s ease-out;
    }
    @keyframes mini-popup-in {
      from { opacity: 0; transform: translateX(-50%) translateY(-4px); }
      to { opacity: 1; transform: translateX(-50%) translateY(0); }
    }
    ```

    4. Make word block position relative (needed for absolute positioning of eye btn and mini popup):
    ```css
    .arcade-viewer-word-block {
      position: relative;
    }
    ```
    Add `position: relative` to the EXISTING `.arcade-viewer-word-block` rule (line ~182 in components.css), do not create a duplicate rule.

    5. Closing cue text style in popup:
    ```css
    .arcade-closing-cue {
      font-size: 1rem;
      letter-spacing: 0.08em;
      color: var(--yellow-300);
      animation: cue-fade-in 0.3s ease-out;
    }
    @keyframes cue-fade-in {
      from { opacity: 0; transform: translateY(6px); }
      to { opacity: 1; transform: translateY(0); }
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/lanle98/Desktop/Coding/imposter_game && node -e "const en=require('./apps/web/src/presentation/locales/en.json'); console.assert(en.wordPopup.closingCue, 'missing closingCue'); console.assert(en.game.peekWord, 'missing peekWord'); console.assert(en.game.wordMasked, 'missing wordMasked'); console.log('i18n OK')"</automated>
  </verify>
  <done>All 4 locale files have closingCue, peekWord, wordMasked keys. components.css has word-pulse keyframes, arcade-eye-btn, arcade-mini-word-popup, arcade-closing-cue classes, and arcade-viewer-word-block has position: relative.</done>
</task>

<task type="auto">
  <name>Task 2: Add closing cue to WordRevealPopup</name>
  <files>apps/web/src/presentation/word-reveal/WordRevealPopup.tsx</files>
  <action>
    Modify WordRevealPopup to show a brief closing cue message before dismissing.

    1. Add `isClosing` state (`useState(false)`).
    2. Add a `useEffect` cleanup ref for the closing timeout to prevent state updates on unmounted component:
       ```typescript
       const closingTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null);
       ```
    3. Add cleanup `useEffect`:
       ```typescript
       useEffect(() => {
         return () => {
           if (closingTimerRef.current) clearTimeout(closingTimerRef.current);
         };
       }, []);
       ```
    4. Create `handleReady` function:
       - Set `isClosing(true)`
       - `closingTimerRef.current = setTimeout(() => onClose(), 1200)`
    5. Change the "Ready!" button `onClick` from `onClose` to `handleReady`.
    6. When `isClosing` is true, replace the popup content (inside `<Popup>`) with the closing cue:
       ```tsx
       <div className="arcade-popup-content">
         <p className="arcade-closing-cue">{t('wordPopup.closingCue')}</p>
       </div>
       ```
       Keep the same `<Popup>` wrapper — only swap inner content.

    Import `useRef` from React (add to existing import).
    Do NOT change the pre-countdown logic at all.
  </action>
  <verify>
    <automated>cd /Users/lanle98/Desktop/Coding/imposter_game && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>WordRevealPopup shows "Word saved to your card" cue for 1.2s before calling onClose. Timeout is cleaned up on unmount. No visible change to the pre-countdown behavior.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project apps/web/tsconfig.json` passes (no type errors)
- All 4 locale JSON files parse without error
- components.css contains word-pulse, arcade-eye-btn, arcade-mini-word-popup, arcade-closing-cue rules
- WordRevealPopup.tsx contains isClosing state and closingTimerRef cleanup
</verification>

<success_criteria>
- Closing cue text appears in WordRevealPopup before dismissal
- All i18n keys present in en/vi/ko/zh
- All CSS classes ready for Plan 02 consumption
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-word-discoverability/04-01-SUMMARY.md`
</output>
